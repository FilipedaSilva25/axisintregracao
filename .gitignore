# Dependências
node_modules/
package-lock.json
yarn.lock

# Arquivos de sistema
.DS_Store
Thumbs.db
desktop.ini

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Arquivos temporários
*.tmp
*.temp
.cache/

# Variáveis de ambiente
.env
.env.2
(2).env
 (2).env
config/selbetti.env
.env.local
.env.*.local

# Backups locais (script gera em backups/ e em Downloads)
backups/

# Repo aninhado (backup-hoje copia o conteúdo)
mylife-os/

# Bot WhatsApp – sessão local (não versionar)
config/data/whatsapp-auth/
config/data/selbetti-portal-password.txt

# Contas AXIS espelhadas no servidor (senhas em texto — não versionar)
config/data/axis-browser-users.json

# Segredos TOTP (não versionar)
config/data/totp-secrets.json

# Selbetti Playwright / fila local (não versionar)
config/data/selbetti-playwright-failure.png
config/data/selbetti-chamados-fila.json

# Notas de sessão local (não versionar)
BLINDAGEM_SESSAO_*.txt
